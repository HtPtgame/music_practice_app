# 🧪 測試指南

## 📋 測試音檔說明

### 測試資料位置
```
assets/test_voice/
├── 測試音檔(midi轉檔).wav    # MIDI轉WAV的標準音檔
├── 測試音檔(環境).wav         # 真實環境錄製
├── 小星星(環境).wav           # 小星星實際演奏
└── 環境背景.wav               # 純背景噪音(無演奏)
```

### 測試案例說明

| # | 測試案例 | 音檔 | 預期結果 | 測試目的 |
|---|---------|------|---------|---------|
| 1 | **MIDI轉檔基準測試** | `測試音檔(midi轉檔).wav` | 95-100% 準確率 | 驗證演算法在理想情況下的表現 |
| 2 | **真實環境錄製測試** | `測試音檔(環境).wav` | 85-95% 準確率 | 測試真實環境下的準確度 |
| 3 | **實際曲目測試** | `小星星(環境).wav` | 視演奏品質 | 測試實際演奏場景 |
| 4 | **噪音抑制測試** | `環境背景.wav` | 0 個正確音符 | 驗證系統抗噪能力 |

---

## 🚀 執行測試

### 階段 2.3 整合測試

**基本使用**:
```powershell
# 執行預設測試 (案例 1: MIDI轉檔基準)
dart test_week3.dart

# 執行特定測試案例
dart test_week3.dart 1    # MIDI轉檔基準測試
dart test_week3.dart 2    # 真實環境錄製測試
dart test_week3.dart 3    # 小星星實際演奏
dart test_week3.dart 4    # 背景噪音抑制測試
```

**測試輸出範例**:
```
╔═══════════════════════════════════════════════════════════╗
║       🎼 階段 2.3: 錯誤分類與整合測試                     ║
╚═══════════════════════════════════════════════════════════╝

📋 可用的測試案例:

   [1] 測試音檔 (MIDI轉檔) - 100%準確基準測試
       → 應該達到 95-100% 準確率 (理想情況)

   [2] 測試音檔 (環境錄製) - 真實演奏測試
       → 測試真實環境下的準確度

   [3] 小星星 (環境錄製) - 實際曲目測試
       → 測試實際演奏準確度

   [4] 環境背景音 - 噪音抑制測試
       → 應該檢測不到任何音符 (驗證抗噪能力)
```

### 階段 2.4 桌面測試

**使用方式**:
```powershell
# 需要將 test.wav 放在桌面
dart test_week4_desktop.dart
```

---

## 📊 測試結果解讀

### 準確度評級

| 準確率 | 評級 | 圖示 | 評價 |
|-------|------|------|------|
| 95-100% | A | 🏆 | 演奏非常出色 |
| 85-94% | B | 🥈 | 演奏很好 |
| 75-84% | C | 🥉 | 需要重點練習錯誤段落 |
| 65-74% | D | 📝 | 建議放慢速度練習 |
| <65% | F | 💪 | 建議分段練習 |

### 測試案例驗證標準

**案例 1: MIDI轉檔基準測試**
- ✅ PASS: 準確度 ≥ 95%
- ⚠️  WARNING: 準確度 < 95%
  - 可能需要調整參數
  - 檢查轉檔品質

**案例 2: 真實環境測試**
- ✅ PASS: 準確度 ≥ 85%
- ⚠️  WARNING: 70% ≤ 準確度 < 85%
  - 可能受環境噪音影響
- ❌ FAIL: 準確度 < 70%
  - 建議檢查錄音環境

**案例 4: 背景噪音測試**
- ✅ PASS: 正確音符 = 0
  - 系統抗噪能力良好
- ⚠️  WARNING: 正確音符 ≤ 總音符 × 10%
  - 可能需要提高能量閾值
- ❌ FAIL: 誤報率 > 10%
  - 需要調整 `energyThreshold` 參數

---

## 🔧 參數調整指南

### 當準確率過低時

**問題**: MIDI轉檔測試 < 95%

**可能原因**:
1. `energyThreshold` 設定過高 → 漏檢
2. `timingTolerance` 設定過窄 → 時間判定過嚴

**調整建議**:
```dart
// 在 note_verification_service.dart 中調整
static const energyThreshold = 0.15;  // 降低 → 0.12
static const timingTolerance = 0.20;  // 放寬 → 0.25
```

### 當誤報率過高時

**問題**: 背景噪音測試檢測到音符

**可能原因**:
1. `energyThreshold` 設定過低 → 誤報
2. 諧波權重設定不當

**調整建議**:
```dart
// 提高能量閾值
static const energyThreshold = 0.15;  // 提高 → 0.20

// 或調整諧波權重
static const harmonicWeights = [1.0, 0.5, 0.25];
// 改為 [1.0, 0.6, 0.3] 增加諧波比重
```

---

## 📈 測試數據記錄

### 建議記錄格式

```markdown
## 測試記錄 - 2025/XX/XX

| 測試案例 | 準確率 | 正確 | 漏音 | 搶拍 | 拖拍 | 評級 | 備註 |
|---------|-------|------|------|------|------|------|------|
| 案例 1 | 97.3% | 218/224 | 6 | 0 | 0 | A | energyThreshold=0.15 |
| 案例 2 | 91.5% | 205/224 | 15 | 3 | 1 | B | 環境噪音稍大 |
| 案例 4 | 100% | 0/0 | 0 | 0 | 0 | A | 無誤報 |

### 參數配置
- energyThreshold: 0.15
- timingTolerance: 0.20
- harmonicWeights: [1.0, 0.5, 0.25]
```

---

## 🎯 測試檢查清單

在每次重大修改後,執行以下測試序列:

- [ ] **案例 1**: MIDI轉檔基準測試
  - [ ] 準確率 ≥ 95%
  - [ ] 處理時間 < 3秒
  
- [ ] **案例 2**: 真實環境測試
  - [ ] 準確率 ≥ 85%
  - [ ] 能處理環境噪音
  
- [ ] **案例 4**: 噪音抑制測試
  - [ ] 誤報率 < 10%
  - [ ] 正確識別無演奏狀態

- [ ] **整合測試**
  - [ ] 所有測試案例無崩潰
  - [ ] 錯誤分類正確
  - [ ] 評級系統運作正常

---

## 💡 故障排除

### 找不到測試檔案

**錯誤訊息**:
```
❌ 找不到 WAV: assets/test_voice/測試音檔(midi轉檔).wav
```

**解決方案**:
1. 確認檔案存在於正確位置
2. 檢查檔案名稱是否正確(注意括號)
3. 確認專案路徑設定

### 測試崩潰

**可能原因**:
1. WAV 檔案格式不正確
2. 記憶體不足
3. FFT 庫未正確安裝

**解決步驟**:
```powershell
# 1. 重新安裝依賴
flutter pub get

# 2. 清除快取
flutter clean

# 3. 重新執行測試
dart test_week3.dart 1
```

---

## 📚 相關文檔

- [AI_WORK_LOG.md](./AI_WORK_LOG.md) - 完整開發歷程
- [README.md](./README.md) - 專案說明
- [TODO.md](./TODO.md) - 待辦事項

---

**最後更新**: 2025年10月8日  
**維護者**: HtPtgame
