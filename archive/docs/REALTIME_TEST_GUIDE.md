# 🎹 即時樂譜比對測試指南

## 📋 測試目的
驗證即時樂譜比對功能是否正確整合並運作。

## 🔍 預期日誌輸出

### 1. 應用啟動時
```
🎹 PracticePage initState: widget.file = xxx.mid, path = /path/to/file.mid
🎹 開始初始化即時樂譜比對器...
🎹 widget.file = Instance of 'PlatformFile'
🎹 widget.file?.name = xxx.mid
🎹 widget.file?.path = /path/to/file.mid
🎹 MIDI 檔案路徑: /path/to/file.mid
🎹 正在讀取 MIDI 檔案...
🎹 MIDI 檔案大小: XXXX bytes
🎹 正在解析 MIDI...
🎹 解析完成,共 XXX 個事件
🎹 正在篩選 NoteOn 事件...
🎹 找到 XXX 個 NoteOn 事件
✅ 載入 MIDI: XXX 個音符
🎹 正在建立即時比對器...
✅ 即時比對器初始化完成
```

### 2. 如果初始化失敗
可能看到以下訊息之一:
```
⚠️ widget.file 為 null,跳過即時比對初始化
⚠️ widget.file.path 為 null,跳過即時比對初始化
⚠️ MIDI 檔案不存在: /path/to/file.mid
⚠️ MIDI 檔案中沒有音符
❌ 即時比對器初始化失敗: [錯誤訊息]
```

### 3. 延遲初始化 (如果 initState 時 file 為 null)
```
🎹 在 build 中檢測到 MIDI 檔案,嘗試延遲初始化比對器
[然後會看到上面的初始化日誌]
```

### 4. 開始錄音時 (啟用即時比對)
```
🎵 啟用即時比對 - 使用 stream 模式錄音
🎵 開始即時音頻處理
```

### 5. 錄音中 (每 500ms)
```
🎵 處理了 X 個檢測音符
```

### 6. 停止錄音時
```
🛑 停止即時音頻處理
```

## ✅ 測試步驟

### 階段 1: 驗證初始化
1. **重新啟動應用**
   ```bash
   flutter run
   ```

2. **選擇 MIDI 檔案並進入練習頁面**

3. **檢查控制台日誌**:
   - ✅ 應該看到 "🎹 開始初始化即時樂譜比對器..."
   - ✅ 應該看到 "✅ 即時比對器初始化完成"
   - ✅ 應該看到 MIDI 解析的詳細過程

4. **檢查 UI**:
   - ✅ 在錄音控制區應該看到「倒數計時」開關
   - ✅ 在倒數計時開關下方應該看到「即時樂譜比對」開關
   - ✅ 如果沒有看到「即時樂譜比對」開關,說明初始化失敗

### 階段 2: 測試即時比對
1. **開啟「即時樂譜比對」開關**
   - 開關應該從灰色變為藍色

2. **點擊「開始錄音」**
   - 控制台應該顯示: "🎵 啟用即時比對 - 使用 stream 模式錄音"
   - 控制台應該顯示: "🎵 開始即時音頻處理"

3. **錄音中彈奏/播放音符**
   - 每 500ms 應該看到: "🎵 處理了 X 個檢測音符"
   - 即時反饋 Widget 應該出現在錄音按鈕下方
   - 應該看到:
     - 📊 進度條
     - 🎯 準確率
     - 🔔 最新判定 (✅正確/❌錯誤/⏭️跳過/🔇雜訊)
     - 📈 統計數字

4. **停止錄音**
   - 控制台應該顯示: "🛑 停止即時音頻處理"
   - 即時反饋 Widget 應該消失

### 階段 3: 驗證後處理分析
1. **點擊「開始分析」按鈕**
   - 應該能正常進行 PerformanceAnalyzer 分析
   - 兩種分析應該可以並存

## 🐛 故障排除

### 問題 1: 看不到「即時樂譜比對」開關
**可能原因**:
- MIDI 檔案未正確載入
- 初始化失敗但錯誤被靜默

**解決方法**:
1. 檢查控制台是否有 "❌ 即時比對器初始化失敗" 訊息
2. 確認 widget.file 和 widget.file.path 不為 null
3. 查看是否有 MIDI 解析錯誤

### 問題 2: 開關存在但錄音時沒有即時反饋
**可能原因**:
- 音頻流未正確啟動
- OptimizedNoteDetectorService 出錯
- Spectrogram 建構失敗

**解決方法**:
1. 確認看到 "🎵 啟用即時比對 - 使用 stream 模式錄音"
2. 確認看到 "🎵 開始即時音頻處理"
3. 檢查是否有 "❌ 音頻處理錯誤" 訊息

### 問題 3: 有即時反饋但結果不準確
**可能原因**:
- 音頻品質問題
- 參數設定不適合當前環境
- 檢測閾值過高/過低

**解決方法**:
1. 確保在安靜環境中測試
2. 檢查 minEnergy, minHarmonicRatio 等參數
3. 嘗試調整 searchWindow 大小

## 📊 預期結果

### 成功的測試應該顯示:
- ✅ 初始化日誌完整
- ✅ UI 開關出現
- ✅ 錄音時看到處理日誌
- ✅ 即時反饋 Widget 正常顯示
- ✅ 統計數字實時更新
- ✅ 判定結果正確顯示

### 當前限制:
- ⚠️ 約 500-800ms 延遲 (處理週期 + FFT)
- ⚠️ 檢測準確率取決於音頻品質
- ⚠️ 僅支援單聲道輸入

## 📝 回報問題時請提供:
1. 完整的控制台日誌 (從啟動到測試結束)
2. 使用的 MIDI 檔案資訊
3. 錄音環境描述
4. 截圖 (UI 狀態)
5. 預期行為 vs 實際行為

---

**測試日期**: 2025/12/25  
**版本**: v1.0.0 (初始整合版)
